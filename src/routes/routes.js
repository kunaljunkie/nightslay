// const { login, getResellerbyUserid, findByIduser, createNewtokenByrefresh } = require("../controllers/login");
// const { getchart } = require("../controllers/reseller");
// const authenticateToken = require("../middlewares/authentication");
// const requestLogger = require("../middlewares/reqReslogger");
// const { validateBody, validateQuery } = require("../middlewares/validations");

// const router = require("express").Router();
// router.use(requestLogger);

// router.post("/login", validateBody, login);
// router.get(
//   "/get-reseller",
//   authenticateToken,
//   validateQuery,
//   getResellerbyUserid
// );
// router.get("/get-chart", authenticateToken, validateQuery, getchart);
// router.get("/user-check", authenticateToken,findByIduser)
// router.get("/new-session", authenticateToken,createNewtokenByrefresh)


// module.exports = router;
